package com.signalspoc.connector.svc.api;

import com.signalspoc.connector.api.ConnectorService;

/**
 * SVC tools (GitHub, GitLab, Bitbucket). Implemented when SVC module is built.
 */
public interface SvcConnectorService extends ConnectorService {
    // List<PullRequest> getOpenPullRequests();
    // List<String> extractLinkedIssueIds(PullRequest pr);
}
